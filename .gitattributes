# Mark the pre-built bundle as generated so GitHub excludes it from language stats
index.html linguist-generated=true

# Shell hooks must keep LF endings — CRLF breaks the shebang on Linux/macOS
.githooks/* text eol=lf
